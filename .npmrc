# React 19 peer-dep compatibility
# Several dependencies (lucide-react@0.294, cmdk@0.2, @radix-ui packages) declare
# peer deps for React ≤18 but work correctly at runtime with React 19.
# Remove this line once all transitive packages update their peerDependencies.
legacy-peer-deps=true
