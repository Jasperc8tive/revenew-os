import { FeaturePlaceholder } from '@/components/layout/FeaturePlaceholder';

export default function SettingsPage() {
  return (
    <FeaturePlaceholder
      title="Settings"
      description="Manage organization defaults, user access, integrations, and workspace preferences."
    />
  );
}
